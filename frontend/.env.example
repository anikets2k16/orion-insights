# URL of the deployed ORION Python backend (FastAPI). Leave blank for local dev
# (the Vite proxy forwards /api → http://localhost:8000). In Lovable / production, set this
# to your backend's public URL, e.g. https://orion-api.onrender.com
VITE_API_URL=
